<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="AdminMapper">
	<select id="selectList">
		SELECT id, password, name, email, tel FROM admin
	</select>

	<select id="selectItem">
		SELECT id, password, name, email, tel FROM admin WHERE email=#{email} and password=#{password}
	</select>

	<insert id="insertItem">
		INSERT INTO admin (password, name, email, tel) VALUES (#{password}, #{name}, #{email}, #{tel})
	</insert>

	<delete id="deleteItem">
		DELETE FROM admin WHERE id=#{id}
	</delete>

</mapper>