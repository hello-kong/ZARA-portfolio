<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="NoticeMapper">
	<select id="selectList">
		SELECT id, title, CONVERT(content USING utf8) AS content, reg_date, edit_date FROM notice
		<where>
			<if test="title != null and title != ''">
				title LIKE concat('%', #{title}, '%')
			</if>

			<if test="content != null and content != ''">
				OR content LIKE concat('%', #{content}, '%')
			</if>
		</where>
			ORDER BY id DESC
			<if test="listCount != 0">
            	LIMIT ${offset}, ${listCount}
        	</if>
	</select>

	<select id="selectItem">
		SELECT id, title, CONVERT(content USING utf8) AS content, reg_date, edit_date FROM notice
		WHERE id=#{id}
	</select>

	<select id="selectNum">
		SELECT id, title, CONVERT(content USING utf8) AS content, reg_date, edit_date FROM notice
		WHERE id IN ((SELECT id FROM notice WHERE id&lt;#{id} ORDER BY id DESC LIMIT 1), (SELECT id FROM notice WHERE id&gt;#{id} ORDER BY id ASC LIMIT 1))
	</select>

	<insert id="insertItem">
		INSERT INTO notice (title, content, reg_date, edit_date) VALUES (#{title}, #{content}, #{reg_date}, #{edit_date})
	</insert>

	<delete id="deleteItem">
		DELETE FROM notice WHERE id=#{id}
	</delete>

	<update id="updateItem">
		UPDATE notice SET title=#{title}, content=#{content}, edit_date=#{edit_date} WHERE id=#{id}
	</update>

	<select id="selectCountAll">
        SELECT COUNT(*) AS cnt FROM notice
		
        <where>
			<if test="title != null and title != ''">
				title LIKE concat('%', #{title}, '%')
			</if>

			<if test="content != null and content != ''">
				OR content LIKE concat('%', #{content}, '%')
			</if>
		</where>
    </select>
</mapper>