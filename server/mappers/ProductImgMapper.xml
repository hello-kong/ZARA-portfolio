<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ProductImgMapper">

	<select id="selectList">
		SELECT color, path FROM product_img WHERE product_id=#{product_id}
	</select>

	<insert id="insertItem">
		INSERT INTO product_img (color, path, origin_name, product_id) VALUES (#{color}, #{path}, #{origin_name}, #{product_id})
	</insert>

	<delete id="deleteItem">
		DELETE FROM product_img WHERE product_id=#{id}
			<if test="color != null and color != ''">
				AND color=#{color}
			</if>
	</delete>
	
</mapper>